classname: tsdat.pipeline.ingest.IngestPipeline

triggers:
  - .*humboldt.*\.csv

retriever:
  path: pipelines/example_ingest/config/retriever.yaml

dataset:
  path: pipelines/example_ingest/config/dataset.yaml
  overrides:
    /attrs/location_id: humboldt

quality:
  path: shared/config/basic-quality.yaml

storage:
  path: shared/config/storage.yaml
